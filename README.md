# ObstacleAvoidance
An AI-based obstacle detection and avoidance system reads input images to identify objects in the path. Using computer vision, it detects obstacles, estimates their position, and triggers avoidance actions, enabling safe and autonomous navigation in dynamic environments.
